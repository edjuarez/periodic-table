# Periodic Table Database

FreeCodeCamp Periodic Table Database project using Bash and PostgreSQL.

## Usage

```bash
./element.sh 1
./element.sh H
./element.sh Hydrogen
```

## Examples

The script accepts an atomic number, symbol, or element name.
